package com.inrik.resource;


import com.inrik.domain.ItemInfo;

public class UserInfoConverter {
	public ItemInfo convert(ItemInfo item){
		//ItemClient itemClient = new ItemClient();
		/*itemClient.setPrice(item.getPrice());
		itemClient.setTransmission(item.getTransmission());
		itemClient.setType(item.getType());
		//itemClient.setVersion(item.getVersion());
		itemClient.setCurrencySymbol(item.getCurrencySymbol());
		//AdvertisementImageInfo images = item.getAdvertisementImageInfo();
		//itemClient.setImage1(images.getImage1());
		
		//itemClient.setAdvertisementId(car.getAdvertisementId());
		//itemClient.setApproved(false);
		//itemClient.setType(car.getType());
		//itemClient.setDetails(item.getDetails());
		//itemClient.setExpireDate(item.getExpireDate());
		//itemClient.setStartDate(item.getStartDate());
		//itemClient.setDeleteCode(item.getDeleteCode());
		//itemClient.setPaid(item.getPaid());
		//AddressInfo adPrincipalInfo = car.getAdPrincipalInfo();
		//carTodo.setAdvertismentType(adPrincipalInfo.getAdvertisement());
		itemClient.setCity(adPrincipalInfo.getCity());
		itemClient.setContactLastname(adPrincipalInfo.getContactLastname());
		itemClient.setContactName(adPrincipalInfo.getContactName());
		itemClient.setCountry(adPrincipalInfo.getCountry());
		itemClient.setEmailAddress(adPrincipalInfo.getEmailAddress());
		//itemClient.setAdvertisementId(car.getAdvertisementId());
		itemClient.setPhoneNumber(adPrincipalInfo.getPhoneNumber());
		itemClient.setState(adPrincipalInfo.getState());
		itemClient.setZipCode(adPrincipalInfo.getZipcode());
		itemClient.setStreetAddress(adPrincipalInfo.getAddress());
		*/
		return null;
		
	}
}
